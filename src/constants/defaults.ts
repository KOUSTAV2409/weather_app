/**
 * Canonical suggested city label (replaces legacy typo / bad defaults).
 * Use for copy and hints — the app does not auto-fetch until the user searches.
 */
export const DEFAULT_CITY_DISPLAY = 'New York';
